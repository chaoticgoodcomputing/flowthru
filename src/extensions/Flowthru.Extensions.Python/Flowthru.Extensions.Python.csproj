<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Flowthru.Extensions.Python</RootNamespace>
    <Description>Python node integration for Flowthru data pipelines</Description>
    
    <!-- NuGet Package Configuration -->
    <PackageId>Flowthru.Extensions.Python</PackageId>
    <PackageTags>data-engineering;pipeline;python;pythonnet</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <!-- Expose internal test helpers to the Python test project -->
    <AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
      <_Parameter1>Flowthru.Extensions.Python.Tests</_Parameter1>
    </AssemblyAttribute>
  </ItemGroup>

  <ItemGroup>
    <!-- Core Flowthru library -->
    <ProjectReference Include="$(RepoRoot)src/core/Flowthru.Core/Flowthru.Core.csproj" />
    
    <!-- Source generator (isolated from runtime dependencies) -->
    <ProjectReference Include="$(RepoRoot)src/extensions/Flowthru.Extensions.Python.SourceGenerators/Flowthru.Extensions.Python.SourceGenerators.csproj"
                      OutputItemType="Analyzer"
                      ReferenceOutputAssembly="false" />
  </ItemGroup>

  <ItemGroup>
    <!-- Python.NET for embedded Python runtime (official package) -->
    <PackageReference Include="pythonnet" />
    
    <!-- Apache Arrow for tabular data interchange (Phase 3) -->
    <PackageReference Include="Apache.Arrow" />
    
    <!-- MSBuild task authoring (Phase 5) -->
    <PackageReference Include="Microsoft.Build.Utilities.Core" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <!-- Include README and LICENSE in package -->
    <None Include="README.md" Pack="true" PackagePath="/" />
    <None Include="$(RepoRoot)LICENSE" Pack="true" PackagePath="/" />
    
    <!-- Include MSBuild targets file in package -->
    <None Include="build\Flowthru.Extensions.Python.targets" Pack="true" PackagePath="build\" />
    
    <!-- Include Arrow bridge module for Python interop -->
    <Content Include="build\_flowthru_arrow.py" 
             CopyToOutputDirectory="PreserveNewest" 
             Link="_flowthru_arrow.py"
             Pack="true" 
             PackagePath="build\" />

    <!-- Include subprocess worker script for isolated per-service execution -->
    <Content Include="build\flowthru_worker.py"
             CopyToOutputDirectory="PreserveNewest"
             Link="flowthru_worker.py"
             Pack="true"
             PackagePath="build\" />
    
    <!-- Include flowthru Python package -->
    <Content Include="build\flowthru\__init__.py" 
             CopyToOutputDirectory="PreserveNewest" 
             Link="flowthru\__init__.py"
             Pack="true" 
             PackagePath="build\flowthru\" />
    <Content Include="build\flowthru\step.py" 
             CopyToOutputDirectory="PreserveNewest" 
             Link="flowthru\step.py"
             Pack="true" 
             PackagePath="build\flowthru\" />
  </ItemGroup>

  <ItemGroup>
    <!-- Bundle Flowthru.Core.dll in the package build/ folder so MSBuild can resolve
         it as a peer assembly when loading PythonSchemaExportTask at build time.
         Without this, the task DLL fails to load in NuGet consumption scenarios
         because the task assembly loader only probes the directory of the task DLL.
         $(OutDir) already includes the TFM segment (e.g. net10.0/), so no double-nesting. -->
    <None Include="$(OutDir)Flowthru.Core.dll"
          Pack="true"
          PackagePath="build\"
          Visible="false" />
    <None Include="$(OutDir)Flowthru.Core.dll"
          Pack="true"
          PackagePath="lib\net10.0\"
          Visible="false" />
  </ItemGroup>

  <!-- Package source generators in standard analyzer location -->
  <ItemGroup>
    <None Include="$(RepoRoot)dist/src/extensions/Flowthru.Extensions.Python.SourceGenerators/$(Configuration)/netstandard2.0/Flowthru.Extensions.Python.SourceGenerators.dll"
          Pack="true"
          PackagePath="analyzers/dotnet/cs"
          Visible="false" />
  </ItemGroup>

  <!-- Ensure source generators are built before packing -->
  <Target Name="EnsureAnalyzerDllsBuilt" BeforeTargets="GenerateNuspec" Condition="'$(NoBuild)' != 'true'">
    <MSBuild Projects="$(RepoRoot)dist/src/extensions/Flowthru.Extensions.Python.SourceGenerators/Flowthru.Extensions.Python.SourceGenerators.csproj"
             Targets="Build"
             Properties="Configuration=$(Configuration)" />
  </Target>

</Project>
