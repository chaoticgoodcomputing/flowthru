# Flowthru-owned local state (Inspector snapshots, cache manifests, etc.)
.flowthru/

# Generated metadata
Metadata/

# Offline JSON-backed Sheets store: raw.json is the checked-in input; the
# working copy each run derives from it (and the flow mutates) is generated.
Data/.local-sheets/working.json
