namespace KedroIrisPython.Data;

/// <summary>
/// Intermediate data layer: Typed representation of raw data (e.g., strings → proper types).
/// Currently unused in this pipeline but provided as a template for downstream users.
/// </summary>
public partial class Catalog
{
    // No entries defined for this layer in the Iris example.
    // This layer would typically contain data where raw string fields have been
    // parsed into proper types (dates, numbers, enums) but no business logic applied.
}
