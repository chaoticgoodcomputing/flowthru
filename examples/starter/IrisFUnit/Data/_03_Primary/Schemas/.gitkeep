# This directory is reserved for primary data layer schemas.
# Schemas would typically define cleaned, validated data structured
# according to domain models (e.g., Customer, Product, Transaction entities).
