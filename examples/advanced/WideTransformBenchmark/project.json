{
  "name": "WideTransformBenchmark",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "examples/advanced/WideTransformBenchmark",
  "tags": [ "scope:examples", "scope:advanced" ],
  "targets": {
    "run": {
      "//": "Fabricate the datasets if missing, measure the optimize pass through both transform paths, and render benchmark_report.md. Sizes via the FLOWTHRU_WTB_SIZES env var.",
      "executor": "nx:run-commands",
      "options": {
        "commands": [ "dotnet run" ],
        "cwd": "examples/advanced/WideTransformBenchmark",
        "parallel": false
      }
    }
  }
}
