[project]
name = "flowthru-python-extension-tests"
version = "0.1.0"
description = "Test environment for Flowthru Python extension"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0.0",
    "pyarrow>=18.0.0",
]

[tool.uv]
dev-dependencies = []
