{
  "name": "tests",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "tests",
  "targets": {
    "test": {
      "//": "Public meta-test barrel. Composes private _test:* subtargets via dependsOn — has no executor body of its own. Each subtarget enforces one structural workspace invariant. See docs/scratch/data-extension-contract.md §6 'Workspace Target Architecture' for the full pattern; tests/README.md describes individual invariants. Adding a new invariant: drop a script under scripts/_test/, add a `_test:<name>` private target below, append to `dependsOn` here.",
      "executor": "nx:noop",
      "dependsOn": [
        "_test:project-mirror",
        "_test:dead-schemas",
        "_test:dead-fixtures",
        "_test:diagnostic-id-registration",
        "_test:package-versions"
      ]
    },
    "_test:project-mirror": {
      "//": "Every src/{domain}/{Project} has a corresponding tests/{domain}/{Project}.Tests at the mirrored path. Private subtarget: invoked only by the tests:test barrel.",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/src/**/*.csproj",
        "{workspaceRoot}/tests/**/*.csproj",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/project-mirror.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/project-mirror.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "_test:dead-schemas": {
      "//": "Every type declared under tests/helpers/Flowthru.Tests.Kits/Schemas/ is referenced as a type argument by at least one *Conformance<...> subclass. Catches schemas authored without follow-through. Private subtarget.",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/tests/helpers/Flowthru.Tests.Kits/Schemas/**/*.cs",
        "{workspaceRoot}/tests/extensions/**/*.cs",
        "{workspaceRoot}/tests/core/**/*.cs",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/dead-schemas.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/dead-schemas.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "_test:dead-fixtures": {
      "//": "Every fixture file under tests/helpers/Flowthru.Tests.Kits/Fixtures/ is referenced (by literal path) from at least one conformance subclass. Catches fixtures authored without wired-up consumers. Private subtarget.",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/tests/helpers/Flowthru.Tests.Kits/Fixtures/**/*.json",
        "{workspaceRoot}/tests/extensions/**/*.cs",
        "{workspaceRoot}/tests/core/**/*.cs",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/dead-fixtures.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/dead-fixtures.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "_test:diagnostic-id-registration": {
      "//": "Every FT### / FU### diagnostic ID emitted from src/ is registered in some AnalyzerReleases.{Un,}shipped.md. Workspace-level backstop for Roslyn's release-tracking analyzer. Private subtarget.",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/src/**/*.cs",
        "{workspaceRoot}/src/**/AnalyzerReleases.Unshipped.md",
        "{workspaceRoot}/src/**/AnalyzerReleases.Shipped.md",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/diagnostic-id-registration.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/diagnostic-id-registration.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "_test:capability-matrix-freshness": {
      "//": "QUARANTINED pending FP-rewrite migration: the capability-matrix.cs script references legacy namespaces (Flowthru.Core.Data.Capabilities.FormatRowFeatures, Flowthru.Core.Abstractions, etc.) that don't exist post-rewrite. The committed capability-matrix.md was generated before the rewrite and may now drift from the new RowFeatures/StorageTraits surface. Reactivate alongside the §4.6 architecture-test scripts when the post-rewrite capability surface stabilizes; until then this target exists for manual invocation but is not in the tests:test dependsOn. Regenerates docs/reference/extensions/capability-matrix.md via the file-based C# program at scripts/_test/capability-matrix.cs and asserts the regenerated content matches the committed file. Drift means a format's RowFeatures, [OptOutOfPropertyPlanner] reason, or implemented capability segment changed without a paired matrix update. Private subtarget. The same generator script is invoked by docs:_build-capability-matrix; freshness is a workspace-discipline concern (here), generation is a docs-build concern (in docs:project.json).",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Capabilities/FormatRowFeatures.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/IFormatBase.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/IFormatRowReader.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/IFormatRowWriter.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/IFormatStreamReader.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/IFormatSerializer.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Serialization/OptOutOfPropertyPlannerAttribute.cs",
        "{workspaceRoot}/src/core/Flowthru.Core/Data/Storage/Format/JsonFormatSerializer.cs",
        "{workspaceRoot}/src/extensions/Flowthru.Extensions.Csv/Data/Storage/Format/CsvFormatSerializer.cs",
        "{workspaceRoot}/src/extensions/Flowthru.Extensions.Excel/Data/Storage/Format/ExcelFormatSerializer.cs",
        "{workspaceRoot}/src/extensions/Flowthru.Extensions.Parquet/Data/Storage/Format/ParquetFormatSerializer.cs",
        "{workspaceRoot}/scripts/_test/capability-matrix.cs",
        "{workspaceRoot}/docs/reference/extensions/capability-matrix.md",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/capability-matrix-freshness.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/capability-matrix-freshness.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "_test:package-versions": {
      "//": "Every <PackageVersion> in Directory.Packages.props declares a major-only floor (N.0.0 for 1+ majors, 0.M.0 for 0-majors) by default. Higher floors require a same-line <!-- ... --> justification comment. Bracketed versions and floating wildcards are forbidden outright. Rationale: every floor here becomes the minimum stamped into the .nuspec of any in-repo package that depends on it, so a tighter-than-necessary floor excludes downstream consumers on otherwise-compatible versions. The escape-hatch comment makes any narrower floor a deliberate, reviewed decision. Private subtarget.",
      "executor": "nx:run-commands",
      "inputs": [
        "{workspaceRoot}/Directory.Packages.props",
        "{workspaceRoot}/scripts/_test/_lib.mjs",
        "{workspaceRoot}/scripts/_test/package-versions.mjs"
      ],
      "options": {
        "commands": [ "node scripts/_test/package-versions.mjs" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "test:integration": {
      "//": "Run only integration-tagged tests (e.g. Postgres-backed EFCore conformance via TestContainers). Requires Docker. Skipped by default in nx run affected -t test; intended for nightly CI runs and on-demand local validation.",
      "executor": "nx:run-commands",
      "inputs": [ "default", "^production" ],
      "options": {
        "commands": [ "dotnet test --filter Category=Integration --no-build" ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "coverage:purge": {
      "//": "Remove all TestResults directories to force a clean test run",
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "find tests -name TestResults -type d -exec rm -rf {} + 2>/dev/null || true"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "coverage:sync": {
      "//": "Sync codecov.yml flags and components from the current project graph and stage the file for commit. Must run before the release commit so the updated codecov.yml is included.",
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node scripts/sync-codecov-flags.mjs"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    },
    "coverage:upload": {
      "//": "Upload all coverage.cobertura.xml files found under tests/ to Codecov. Depends on coverage:sync so dist/codecov-flags.json exists regardless of prior workflow steps — CI used to fail with 'flags.json not found' when the test step bailed before the explicit Sync step ran. Requires CODECOV_TOKEN env var and the codecov CLI on PATH.",
      "executor": "nx:run-commands",
      "dependsOn": [ "coverage:sync" ],
      "options": {
        "commands": [
          "bash scripts/upload-coverage.sh"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      }
    }
  }
}
