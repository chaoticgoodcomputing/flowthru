[project]
name = "flowthru-examples-tests"
version = "0.1.0"
description = "Unified Python environment for all Flowthru example tests"
requires-python = ">=3.10"
dependencies = [
  # Combined dependencies from all Python examples
  "pandas>=2.0.0",
  "pyarrow>=18.0.0",
  "numpy>=1.24.0",
  "scikit-learn~=1.5.1",
  "matplotlib>=3.5.0",
  "seaborn~=0.12.1",
  "plotly>=5.0.0",
]

[tool.uv]
dev-dependencies = []
